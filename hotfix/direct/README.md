# 💀 GAME OVER

You pushed directly to production. Bypassed the pipeline. Skipped staging.

The fix was correct. But it was in a file with a pending merge conflict. The push resolved it by choosing the wrong branch. It undid a security patch from last week.

---

*📖 The Cloudbleed bug (2017): a single errant `==` became `=` in Cloudflare's HTML parser. One character. It leaked sensitive data from 4 million websites for months. The fix was one line. The damage was global. A direct push bypassed the review that would have caught it. Every bypass is a gamble. Eventually, the house wins.*

---

[🔄 Try again from the beginning](https://github.com/shift/left)
